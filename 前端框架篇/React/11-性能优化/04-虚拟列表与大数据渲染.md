# 虚拟列表与大数据渲染

> 渲染 **1 万行 DOM** 即使用户只看 20 行——**虚拟列表（窗口化）** 只 mount 可视区 + 缓冲区的项，滚动时复用节点。

---

## 一、问题本质

```mermaid
flowchart TB
  subgraph bad [全量渲染]
    D1[10000 个 DOM 行]
  end
  subgraph good [虚拟列表]
    V[可视 ~15 行 + buffer]
    V --> Pool[节点复用]
  end
```

| 指标 | 1 万行全渲染 | 虚拟列表 |
|------|--------------|----------|
| DOM 节点 | ~1 万 | ~30 |
| 首次 paint | 慢 | 快 |
| 滚动 | 易掉帧 | 流畅 |

---

## 二、原理

```
┌─────────────────────────────┐  ← 容器 viewport
│  [padding-top 占位]          │
│  ┌ row 100 ┐                │
│  ┌ row 101 ┐  可见区         │
│  ┌ row 102 ┐                │
│  [padding-bottom 占位]       │
└─────────────────────────────┘
总高度 = itemCount × itemHeight（或动态测量）
```

| 概念 | 说明 |
|------|------|
| **scrollTop** | 决定起始 index |
| **itemHeight** | 固定高最简单 |
| **overscan** | 上下多渲染几行防白屏 |

---

## 三、@tanstack/react-virtual

```bash
pnpm add @tanstack/react-virtual
```

```tsx
import { useVirtualizer } from '@tanstack/react-virtual';
import { useRef } from 'react';

function VirtualList({ rows }: { rows: string[] }) {
  const parentRef = useRef<HTMLDivElement>(null);

  const virtualizer = useVirtualizer({
    count: rows.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 36,
    overscan: 5,
  });

  return (
    <div ref={parentRef} style={{ height: 400, overflow: 'auto' }}>
      <div
        style={{
          height: virtualizer.getTotalSize(),
          position: 'relative',
        }}
      >
        {virtualizer.getVirtualItems().map(vRow => (
          <div
            key={vRow.key}
            style={{
              position: 'absolute',
              top: 0,
              transform: `translateY(${vRow.start}px)`,
              height: vRow.size,
            }}
          >
            {rows[vRow.index]}
          </div>
        ))}
      </div>
    </div>
  );
}
```

---

## 四、固定高 vs 动态高

| 固定高 | 动态高 |
|--------|--------|
| `estimateSize: () => 36` | `measureElement` 实测 |
| 实现简单 | 滚动条跳动需处理 |
| 表格行、统一列表项 | 评论、多行文本 |

---

## 五、与表格组件

| 方案 | 说明 |
|------|------|
| **TanStack Table + Virtual** |  head 固定 + body 虚拟 |
| **AG Grid / MUI DataGrid** | 内置虚拟化 |
| 自研 | 仅数据极大时 |

中后台 500 行以内可先不虚拟；**万级**再引入。

---

## 六、其他大数据策略

| 策略 | 场景 |
|------|------|
| **分页** | 服务端分页，最简单 |
| **无限滚动** | 社交 feed，配合 Query |
| **Web Worker** | 排序/过滤在 worker |
| **Canvas** | 超密点图、非 DOM |

见 [09-Query 无限滚动](../09-数据获取与缓存/03-Query与Mutation实战模式.md)。

---

## 七、key 与选中态

虚拟列表**复用 DOM**，行内 state 可能错位：

| 问题 | 解决 |
|------|------|
| 输入框内容串行 | 状态放 data 层，不留在 DOM |
| 选中行 | 用 row id 存 Set |

---

## 八、反模式

| ❌ | ✅ |
|----|-----|
| 100 行就上虚拟 | 先分页 |
| 虚拟 + 复杂 colspan 表格 | 用成熟表格库 |
| 忘记 overscan | 快速滚动白屏 |

---

## 九、小结

| 要点 | |
|------|--|
| 只渲染窗口内 | |
| TanStack Virtual 通用 | |
| 优先分页，万级再虚拟 | |

**上一篇**：[03-Profiler与性能分析](./03-Profiler与性能分析.md)  
**下一篇**：[05-Web-Vitals与体验指标](./05-Web-Vitals与体验指标.md)
